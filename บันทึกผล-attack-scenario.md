# บันทึกผลการทดสอบ Attack Scenario

## ขั้นตอนที่ 3.4 Payload ขนาดใหญ่ผิดปกติ

ได้ทดสอบส่งข้อมูลที่มีขนาดประมาณ 15KB ซึ่งเกินขีดจำกัดที่กำหนดไว้ 10kb ผลคือระบบตอบกลับ `413 Payload Too Large` ทำให้ไม่สามารถส่งข้อมูลขนาดใหญ่เกินกำหนดเข้าสู่ระบบได้ จึงช่วยป้องกันการส่ง Payload ที่ผิดปกติได้ในระดับหนึ่ง

---

## ขั้นตอนที่ 3.5 Type Confusion

ได้ทดสอบส่งค่า name เป็น Object แทน String ผลคือระบบยังยอมรับข้อมูลดังกล่าว เนื่องจาก Validation ปัจจุบันตรวจสอบเพียงว่ามีค่าหรือไม่ แต่ไม่ได้ตรวจสอบชนิดข้อมูล ทำให้ข้อมูลที่ไม่ตรงตามรูปแบบสามารถเข้าสู่ระบบได้

---

### ข้อสังเกต

จากการทดสอบพบว่าการจำกัดขนาด Request สามารถช่วยป้องกัน Payload ที่ใหญ่เกินไปได้ แต่การตรวจสอบชนิดข้อมูลยังไม่รัดกุม จึงควรเพิ่ม Schema Validation เพื่อให้ตรวจสอบทั้ง ข้อมูลที่จำเป็นและประเภทของข้อมูล ก่อนนำไปใช้งานต่อไป
